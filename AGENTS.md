# AGENTS.md

本文件用于约束所有在本仓库内工作的 Agent / AI Coding Assistant。

目标不是让 Agent 自由发挥，而是让 Agent **先理解项目、再遵守边界、再按主链路工作**。

根目录 `AGENTS.md` 只保留前后端都必须共享的最核心规则。 前端专项规则请继续读取 `apps/AGENTS.md`。 后端专项规则请继续读取 `services/AGENTS.md`。

## 必读与优先级

修改产品、架构、代码或文档前，必须阅读：

- 当前 `AGENTS.md`
- 如果是前端代码，读 `apps/AGENTS.md`，如果是后端代码，读 `services/AGENTS.md`;
- 根目录 `README.md`；
- 改动目录内最近的 `README.md`；
- 本次工作关联的 Issue、Proposal 和验收标准。

如果必读文件不存在，不得臆造内容；应继续基于已存在的规则和代码现状工作，并在交付说明中说明缺失。已接受的项目决策和用户最新明确指令优先于仍在讨论的 Proposal。发现规则、Issue、代码现状冲突时，不得静默选择；先以最小范围保留现有行为，并在交付说明中指出冲突。

## 项目边界

### 架构边界

Lingow 采用控制面与媒体面分离的双服务架构：

- `services/api`：负责账户、会话、语言配置、Final Turn 持久化、历史查询、用量汇总和异步消息。
- `services/realtime-audio`：负责 WebRTC 信令、WebRTC 音频、VAD、ASR、翻译、TTS、打断处理和运行时状态机。
- `packages/contracts`：统一维护 REST API、信令、实时事件、错误码和状态定义，是跨模块契约的唯一来源。
- Web、Mobile 和 Device SDK 只负责音频采s集、播放、交互和状态展示，不承载后端业务判断。

各模块代表业务责任划分，不代表独立部署服务。

### 模块边界

| 模块             | 所属服务                  | 负责                                              | 主要交付                       |
| ---------------- | ------------------------- | ------------------------------------------------- | ------------------------------ |
| 会话管理         | `services/api`            | 会话创建、启动、结束及业务状态                    | Session                        |
| 语言配置         | `services/api`            | 支持语言、双语配置和版本管理                      | LanguageConfigSnapshot         |
| 实时转译         | `services/realtime-audio` | WebRTC、VAD、ASR、翻译、TTS 及运行状态            | FinalTurn、UsageFact           |
| 说话人与转译记录 | `services/api`            | Final Turn 持久化、临时说话人、归属修正和历史查询 | VoiceTurn、Participant         |
| 账户、用量与消息 | `services/api`            | 认证、用量汇总和异步消息投递                      | Account、UsageSummary、Message |

### 模块交接规则

1. 会话管理模块创建会话，并使用账户、用量与消息模块提供的 `account_id` 确定会话归属。
2. 语言配置模块管理会话双语配置；实时转译模块在每个 Turn 开始时读取一次配置，并在本轮内固定使用。
3. 实时转译模块处理实时音频，向说话人与转译记录模块提交 `FinalTurn`，向账户、用量与消息模块提交 `UsageFact`。
4. 实时转译模块可以产生临时说话人结果；说话人与转译记录模块负责说话人和 Final Turn 的长期保存及后续归属修正。
5. 说话人与转译记录模块保存 Final Turn；说话人尚未确定时允许 `participant_id = null`，后续异步补充或修正。
6. 账户、用量与消息模块幂等记录 `UsageFact`；消息发送只读取已经保存的 final Turn，并生成不可变内容快照。
7. 跨服务数据必须通过 `packages/contracts` 定义，并支持幂等、重试和可靠投递。

### 状态边界

- 会话创建、启动和结束等业务状态由 `services/api` 管理。
- 听音、识别、翻译、播放和打断等运行状态由 `services/realtime-audio` 管理。
- `services/api` 只查询实时状态快照，不重复维护实时播放状态机。
- `services/realtime-audio` 产生实时事实，`services/api` 负责需要长期保留的业务数据。

## 执行方式

- 先明确当前任务的用户、问题、范围、不做什么、接口或数据影响、可验证验收标准。

- 对范围明确、低风险、可逆的本地修改，直接执行、验证并汇报；不要反复询问是否继续。

- 对破坏性操作、生产环境操作、凭证访问、外部发布、合并、推送主分支、删除大量文件或

  明显扩大范围的行为，必须先获得明确授权。

- 不做用户没有要求的功能、抽象、配置项或未来兼容层。

- 优先复用现有 package、interface、struct、contract、service 和测试模式；不要用目录

  占位、技术栈清单或无法编译的伪骨架冒充架构。

## Git 与 Commit 规范

- 一个 commit 只解决一个明确、独立、可回退的逻辑边界问题。
- 同一功能中可以独立评审、验证和回退的细节应拆成多个 commits；不要为了提交 PR 把无关或可独立验证的改动压成单个提交。
- 与某个实现细节强相关的文档回写、注释修正和测试补充，应与该细节一并提交，或作为紧邻的原子提交。
- 提交前必须检查 `git diff`、`git status` 和 `git diff --staged`，确认只包含本次范围内的改动。
- Commit message 优先使用 `<type>(<scope>): <subject>`；`subject` 用祈使句或简短动词短语描述行为变化，避免空泛的 `update`、`fix stuff`。

## 设计与实现原则

- 最小改动优先。每一行变更都应能对应到当前任务、缺陷、验收标准或必要验证。

- 保持模块边界清晰：后端拥有权威业务状态，桌面端只承载桌面能力和交互，契约层表达跨端

  数据结构。

- 成功路径应清晰向下流动；输入错误、无操作、特殊分支和依赖失败尽早返回。

- 不为单次使用代码新增抽象；只有当抽象明显降低重复、隔离边界或匹配既有模式时才新增。

- 对外部供应商、模型、设备和存储保持可替换边界，不把供应商细节泄漏到核心业务层。

- 配置通过环境变量、配置文件或启动参数进入系统；不得硬编码密钥、路径、账号或真实数据。

- 文档必须跟随实现更新。若改动改变接口、流程、配置、测试方式或用户可见行为，应同步更新

  对应文档或在交付说明中明确文档缺口。

## 测试与验证

- 新功能和缺陷修复必须补充对应测试，覆盖主要成功路径、输入错误路径和依赖失败路径。
- 单元测试默认不得连接真实数据库、Redis、MQ、第三方模型、真实设备或供应商服务。
- 优先使用小型 fake/stub 隔离外部依赖；不得为了简单边界引入复杂 mock 框架。
- 集成测试必须与默认单元测试隔离，默认测试应稳定、离线、可重复运行。
- 不用固定等待时间协调异步或并发测试；应使用确定性同步机制。
- 前后端、桌面和契约变更如果有对应子目录 `AGENTS.md` ，应该分别遵循其专项测试规则。

## 完成标准

- 保持改动小、可解释、可回退，并遵循相邻代码和目录的既有模式。
- Go 改动至少执行 `gofmt` 和相关包测试。
- 交付前优先执行 `make check`；如仓库没有该目标或当前环境无法执行，改为运行最相关的测试、类型检查、构建或静态检查，并说明验证缺口。
- 前端改动应通过仓库已有脚本提供的 format/lint/typecheck/unit/build；影响关键桌面流程时补充 E2E 或说明无法执行原因。
- 契约变化必须同时校验 schema、生成物和前后端消费者；禁止只改单侧并假定兼容。
- 不得通过删除、跳过或弱化测试来制造通过结果。

## 交付说明

最终回复应简明说明：

- 改了什么文件；
- 为什么这样改；
- 执行了哪些验证；
- 哪些验证未能执行以及原因；
- 是否存在规则、代码现状、Issue 或 Proposal 的冲突。
