openapi: 3.1.0
info:
  title: Lingow Account, Usage and Delivery API
  version: 0.1.0
servers:
  - url: /api/v1
paths:
  /auth/anonymous:
    post:
      operationId: createAnonymousAccount
      responses:
        '201': { description: Created, content: { application/json: { schema: { $ref: '#/components/schemas/AuthResult' } } } }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /auth/verification-codes:
    post:
      operationId: createPhoneChallenge
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PhoneChallengeRequest' } } } }
      responses:
        '202': { description: Accepted, content: { application/json: { schema: { type: object, required: [challenge_id], properties: { challenge_id: { type: string } } } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '429': { $ref: '#/components/responses/TooManyRequests' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /auth/phone/login:
    post:
      operationId: verifyPhone
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PhoneVerifyRequest' } } } }
      responses:
        '200': { description: Verified, content: { application/json: { schema: { $ref: '#/components/schemas/AuthResult' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /auth/token/refresh:
    post:
      operationId: refreshToken
      requestBody: { $ref: '#/components/requestBodies/RefreshToken' }
      responses:
        '200': { description: Refreshed, content: { application/json: { schema: { $ref: '#/components/schemas/Tokens' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /auth/logout:
    post:
      operationId: logout
      requestBody: { $ref: '#/components/requestBodies/RefreshToken' }
      responses:
        '204': { description: Logged out }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/me:
    get:
      operationId: getMe
      security: [{ accountContext: [] }]
      responses:
        '200': { description: Current account, content: { application/json: { schema: { $ref: '#/components/schemas/Account' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/device-pairing-codes:
    post:
      operationId: createDevicePairingCode
      security: [{ accountContext: [] }]
      responses:
        '201': { description: One-time pairing code, content: { application/json: { schema: { $ref: '#/components/schemas/DevicePairingCode' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '403': { $ref: '#/components/responses/Forbidden' }
  /account/devices:
    get:
      operationId: listBoundDevices
      security: [{ accountContext: [] }]
      responses:
        '200': { description: Devices bound to the current account, content: { application/json: { schema: { $ref: '#/components/schemas/DeviceList' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /account/devices/{device_id}:
    delete:
      operationId: revokeBoundDevice
      security: [{ accountContext: [] }]
      parameters: [{ name: device_id, in: path, required: true, schema: { type: string, minLength: 1 } }]
      responses:
        '204': { description: Device revoked and its short-lived credentials invalidated }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
  /devices/pair:
    post:
      operationId: pairDevice
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/PairDeviceRequest' } } } }
      responses:
        '200': { description: Device bound to the account that created the pairing code, content: { application/json: { schema: { $ref: '#/components/schemas/Device' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '409': { $ref: '#/components/responses/Conflict' }
  /device-auth/challenges:
    post:
      operationId: createDeviceAuthenticationChallenge
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/DeviceAuthenticationChallengeRequest' } } } }
      responses:
        '201': { description: Short-lived device signature challenge, content: { application/json: { schema: { $ref: '#/components/schemas/DeviceAuthenticationChallenge' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /device-auth/tokens:
    post:
      operationId: exchangeDeviceAuthenticationChallenge
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/DeviceAuthenticationTokenRequest' } } } }
      responses:
        '200': { description: Short-lived device credential, content: { application/json: { schema: { $ref: '#/components/schemas/DeviceAccessToken' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /device/voice-sessions:
    post:
      operationId: createDeviceVoiceSession
      security: [{ deviceContext: [] }]
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateVoiceSessionRequest' } } } }
      responses:
        '201': { description: Session owned by the authenticated device's bound account, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSession' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '409': { $ref: '#/components/responses/Conflict' }
  /device/voice-sessions/{id}/start:
    post:
      operationId: startDeviceVoiceSession
      security: [{ deviceContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }, { $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/StartVoiceSessionRequest' } } } }
      responses:
        '200': { description: Started device session, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSession' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
  /device/voice-sessions/{id}/end:
    post:
      operationId: endDeviceVoiceSession
      security: [{ deviceContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }, { $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: false, content: { application/json: { schema: { $ref: '#/components/schemas/EndVoiceSessionRequest' } } } }
      responses:
        '200': { description: Ended device session, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSession' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
  /device/voice-sessions/{id}/realtime-ticket:
    post:
      operationId: mintDeviceRealtimeTicket
      security: [{ deviceContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }]
      responses:
        '200': { description: Short-lived ticket for the authenticated device session, content: { application/json: { schema: { $ref: '#/components/schemas/RealtimeTicket' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
  /voice-sessions:
    post:
      operationId: createVoiceSession
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/CreateVoiceSessionRequest' }
      responses:
        '201': { description: Created, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSession' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '501': { $ref: '#/components/responses/NotImplemented' }
    get:
      operationId: listVoiceSessions
      security: [{ accountContext: [] }]
      parameters:
        - { name: status, in: query, required: false, schema: { $ref: '#/components/schemas/VoiceSessionStatus' } }
        - { name: cursor, in: query, required: false, schema: { type: string } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        '200': { description: Persistent session page, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSessionListPage' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}:
    get:
      operationId: getVoiceSession
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }]
      responses:
        '200': { description: Session detail with runtime snapshot, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSessionDetail' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /languages:
    get:
      operationId: listSupportedLanguages
      security: [{ accountContext: [] }]
      parameters:
        - { name: active, in: query, required: false, schema: { type: boolean, default: true } }
      responses:
        '200': { description: Supported languages, content: { application/json: { schema: { $ref: '#/components/schemas/ListLanguagesResponse' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/language-config:
    get:
      operationId: getCurrentLanguageConfig
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }]
      responses:
        '200': { description: Active language configuration, content: { application/json: { schema: { $ref: '#/components/schemas/LanguageConfig' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/language-configs:
    get:
      operationId: listLanguageConfigs
      security: [{ accountContext: [] }]
      parameters:
        - { $ref: '#/components/parameters/SessionID' }
        - { name: cursor, in: query, required: false, schema: { type: string } }
        - { name: limit, in: query, required: false, schema: { type: integer, minimum: 1, maximum: 100, default: 20 } }
      responses:
        '200': { description: Language configuration history, content: { application/json: { schema: { $ref: '#/components/schemas/ListLanguageConfigsResponse' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '501': { $ref: '#/components/responses/NotImplemented' }
    post:
      operationId: createLanguageConfig
      security: [{ accountContext: [] }]
      parameters:
        - { $ref: '#/components/parameters/SessionID' }
        - { $ref: '#/components/parameters/IdempotencyKey' }
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateLanguageConfigRequest' } } } }
      responses:
        '201': { description: Created active language configuration, content: { application/json: { schema: { $ref: '#/components/schemas/LanguageConfig' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422':
          description: Output configuration cannot be applied
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Error' }
              examples:
                deliveryTargetRequired:
                  summary: Single output has no ready target
                  value:
                    error:
                      code: delivery_target_required
                      message: single output requires an enabled and verified delivery target
                      request_id: req_language_config
                      retryable: false
                      details: {}
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/start:
    post:
      operationId: startVoiceSession
      security: [{ accountContext: [] }]
      parameters:
        - { $ref: '#/components/parameters/SessionID' }
        - { $ref: '#/components/parameters/IdempotencyKey' }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/StartVoiceSessionRequest' }
      responses:
        '200': { description: Started session, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSession' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/end:
    post:
      operationId: endVoiceSession
      security: [{ accountContext: [] }]
      parameters:
        - { $ref: '#/components/parameters/SessionID' }
        - { $ref: '#/components/parameters/IdempotencyKey' }
      requestBody:
        required: false
        content:
          application/json:
            schema: { $ref: '#/components/schemas/EndVoiceSessionRequest' }
      responses:
        '200': { description: Ended or terminal session, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSession' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/state:
    get:
      operationId: getVoiceSessionState
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }]
      responses:
        '200': { description: Session business and runtime state, content: { application/json: { schema: { $ref: '#/components/schemas/VoiceSessionStateSnapshot' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/mode:
    get:
      operationId: getVoiceSessionMode
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }]
      responses:
        '200': { description: Authoritative realtime mode snapshot, content: { application/json: { schema: { $ref: '#/components/schemas/ModeStateSnapshot' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
        '501': { $ref: '#/components/responses/NotImplemented' }
    post:
      operationId: switchVoiceSessionMode
      security: [{ accountContext: [] }]
      parameters:
        - { $ref: '#/components/parameters/SessionID' }
        - { $ref: '#/components/parameters/IdempotencyKey' }
        - { $ref: '#/components/parameters/RequestID' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SwitchVoiceSessionModeRequest' }
      responses:
        '200': { description: Mode switch result from realtime, content: { application/json: { schema: { $ref: '#/components/schemas/SwitchModeResult' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/realtime-ticket:
    post:
      operationId: mintVoiceSessionRealtimeTicket
      description: >
        Issues a short-lived HMAC realtime ticket for the session owner so the
        browser can join the realtime control plane (WebRTC signaling).
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }]
      responses:
        '200':
          description: Realtime ticket for one session
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RealtimeTicket' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/usage:
    get:
      operationId: getSessionUsage
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }]
      responses:
        '200': { description: Session usage, content: { application/json: { schema: { $ref: '#/components/schemas/UsageSummary' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /voice-sessions/{id}/automatic-output-status:
    get:
      operationId: listAutomaticOutputStatus
      description: Recent durable automatic-output states for one owned voice session. `fallback_pending` means fallback playback is claimed, `fallback_played` means the fallback was accepted, and `restored` means bidirectional output was restored with the configuration compare-and-swap guard.
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/SessionID' }]
      responses:
        '200': { description: Recent automatic-output recovery states, content: { application/json: { schema: { $ref: '#/components/schemas/AutomaticOutputStatusListResponse' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /usage/summary:
    get:
      operationId: getAccountUsage
      security: [{ accountContext: [] }]
      parameters:
        - { name: period_start, in: query, required: true, schema: { type: string, format: date-time } }
        - { name: period_end, in: query, required: true, schema: { type: string, format: date-time } }
      responses:
        '200': { description: Account usage, content: { application/json: { schema: { $ref: '#/components/schemas/UsageSummary' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /outbound-messages:
    get:
      operationId: listOutboundMessages
      security: [{ accountContext: [] }]
      responses:
        '200': { description: Recent account messages, content: { application/json: { schema: { $ref: '#/components/schemas/OutboundMessageListResponse' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
    post:
      operationId: createOutboundMessage
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/IdempotencyKey' }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/CreateMessageRequest' } } } }
      responses:
        '202': { description: Queued, content: { application/json: { schema: { $ref: '#/components/schemas/Message' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /outbound-messages/{message_id}:
    get:
      operationId: getOutboundMessage
      security: [{ accountContext: [] }]
      parameters: [{ $ref: '#/components/parameters/MessageID' }]
      responses:
        '200': { description: Message, content: { application/json: { schema: { $ref: '#/components/schemas/Message' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /outbound-deliveries/{message_id}/retry:
    post:
      operationId: retryOutboundMessage
      security: [{ accountContext: [] }]
      parameters:
        - { $ref: '#/components/parameters/MessageID' }
        - { $ref: '#/components/parameters/IdempotencyKey' }
      responses:
        '202': { description: Retry queued, content: { application/json: { schema: { $ref: '#/components/schemas/Message' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/message-preferences:
    get:
      operationId: listMessagePreferences
      security: [{ accountContext: [] }]
      responses:
        '200': { description: Preferences, content: { application/json: { schema: { $ref: '#/components/schemas/MessagePreferenceListResponse' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/automatic-delivery-readiness:
    get:
      operationId: getAutomaticDeliveryReadiness
      security: [{ accountContext: [] }]
      responses:
        '200': { description: Automatic delivery readiness for single output, content: { application/json: { schema: { $ref: '#/components/schemas/AutomaticDeliveryReadiness' } } } }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/message-preferences/{channel}/{destination_ref}:
    put:
      operationId: putMessageTargetPreference
      security: [{ accountContext: [] }]
      parameters:
        - { name: channel, in: path, required: true, schema: { $ref: '#/components/schemas/Channel' } }
        - { name: destination_ref, in: path, required: true, schema: { type: string, minLength: 1 } }
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/UpdateMessagePreferenceRequest' } } } }
      responses:
        '200': { description: Updated, content: { application/json: { schema: { $ref: '#/components/schemas/Preference' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/message-targets:
    get:
      operationId: listMessageTargets
      security: [{ accountContext: [] }]
      parameters: [{ name: channel, in: query, required: false, schema: { $ref: '#/components/schemas/Channel' } }]
      responses:
        '200': { description: Bound targets, content: { application/json: { schema: { $ref: '#/components/schemas/MessageTargetListResponse' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/message-targets/wechat/bind:
    post:
      operationId: bindWeChatTarget
      security: [{ accountContext: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/BindWeChatRequest' } } } }
      responses:
        '200': { description: Bound target, content: { application/json: { schema: { $ref: '#/components/schemas/MessageTarget' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/message-targets/wechat/{destination_ref}:
    delete:
      operationId: unbindWeChatTarget
      security: [{ accountContext: [] }]
      parameters: [{ name: destination_ref, in: path, required: true, schema: { type: string } }]
      responses:
        '204': { description: Target revoked }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/message-targets/email/verification-codes:
    post:
      operationId: requestEmailBindVerification
      security: [{ accountContext: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              required: [email]
              properties:
                email: { type: string, minLength: 3, format: email }
                destination_ref: { type: string, minLength: 1 }
      responses:
        '202': { description: Verification token accepted for delivery }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/message-targets/email/bind:
    post:
      operationId: bindEmailTarget
      security: [{ accountContext: [] }]
      requestBody: { required: true, content: { application/json: { schema: { $ref: '#/components/schemas/BindEmailRequest' } } } }
      responses:
        '200': { description: Bound target, content: { application/json: { schema: { $ref: '#/components/schemas/MessageTarget' } } } }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /account/message-targets/email/{destination_ref}:
    delete:
      operationId: unbindEmailTarget
      security: [{ accountContext: [] }]
      parameters: [{ name: destination_ref, in: path, required: true, schema: { type: string } }]
      responses:
        '204': { description: Target revoked }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '501': { $ref: '#/components/responses/NotImplemented' }
  /realtime/v1/sessions/{session_id}/start:
    servers:
      - url: /
    post:
      operationId: startRealtimeSession
      security:
        - realtimeTicket: []
      parameters:
        - { name: session_id, in: path, required: true, schema: { type: string } }
        - { $ref: '#/components/parameters/IdempotencyKey' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RealtimeStartRequest' }
      responses:
        '200':
          description: Runtime started
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RealtimeRuntimeSnapshot' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '409':
          description: Runtime is owned by another Start operation
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RuntimeOperationConflictError' }
  /realtime/v1/sessions/{session_id}/stop:
    servers:
      - url: /
    post:
      operationId: stopRealtimeSession
      security:
        - realtimeTicket: []
      parameters:
        - { name: session_id, in: path, required: true, schema: { type: string } }
        - { $ref: '#/components/parameters/IdempotencyKey' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/RealtimeStopRequest' }
      responses:
        '200':
          description: Runtime stopped
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RealtimeRuntimeSnapshot' }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /realtime/v1/sessions/{session_id}/fallback-playback:
    servers:
      - url: /
    post:
      operationId: acceptFallbackPlayback
      security:
        - realtimeTicket: []
      parameters:
        - { name: session_id, in: path, required: true, schema: { type: string } }
        - { $ref: '#/components/parameters/IdempotencyKey' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/FallbackPlaybackRequest' }
      responses:
        '202':
          description: Fallback playback accepted durably or accepted previously
          content:
            application/json:
              schema: { $ref: '#/components/schemas/FallbackPlaybackReceipt' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/NotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /realtime/v1/sessions/{session_id}/runtime:
    servers:
      - url: /
    get:
      operationId: getRealtimeRuntimeSnapshot
      security:
        - realtimeTicket: []
      parameters:
        - { name: session_id, in: path, required: true, schema: { type: string } }
      responses:
        '200':
          description: Current runtime snapshot
          content:
            application/json:
              schema: { $ref: '#/components/schemas/RealtimeRuntimeSnapshot' }
        '401': { $ref: '#/components/responses/Unauthorized' }
  /realtime/v1/sessions/{session_id}/mode:
    servers:
      - url: /
    get:
      operationId: getRealtimeModeState
      security:
        - realtimeTicket: []
      parameters:
        - { name: session_id, in: path, required: true, schema: { type: string } }
      responses:
        '200':
          description: Current authoritative mode state for the realtime runtime
          content:
            application/json:
              schema: { $ref: '#/components/schemas/ModeStateSnapshot' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/RuntimeNotFound' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
    post:
      operationId: switchRealtimeMode
      security:
        - realtimeTicket: []
      parameters:
        - { name: session_id, in: path, required: true, schema: { type: string } }
        - { $ref: '#/components/parameters/IdempotencyKey' }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/SwitchModeCommand' }
      responses:
        '200':
          description: Mode switch applied, replayed, or already in the requested mode
          content:
            application/json:
              schema: { $ref: '#/components/schemas/SwitchModeResult' }
        '400': { $ref: '#/components/responses/BadRequest' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404': { $ref: '#/components/responses/RuntimeNotFound' }
        '409': { $ref: '#/components/responses/Conflict' }
        '422': { $ref: '#/components/responses/UnprocessableEntity' }
        '503': { $ref: '#/components/responses/ServiceUnavailable' }
  /realtime/v1/sessions/{session_id}/connection:
    servers:
      - url: /
    get:
      operationId: getRealtimeConnectionSnapshot
      security:
        - realtimeTicket: []
      parameters:
        - { name: session_id, in: path, required: true, schema: { type: string } }
      responses:
        '200':
          description: Current WebRTC connection snapshot
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WebRTCConnectionSnapshot' }
        '401': { $ref: '#/components/responses/Unauthorized' }
        '404':
          description: No current WebRTC connection exists for the Session
          content:
            application/json:
              schema: { $ref: '#/components/schemas/WebRTCConnectionError' }
  /realtime/v1/sessions/{session_id}/webrtc/config:
    servers: [{ url: / }]
    get:
      operationId: getRealtimeWebRTCConfig
      security: [{ realtimeTicket: [] }]
      parameters: [{ name: session_id, in: path, required: true, schema: { type: string } }]
      responses: { '200': { description: WebRTC connection configuration, content: { application/json: { schema: { $ref: '#/components/schemas/WebRTCConfig' } } } }, '401': { $ref: '#/components/responses/Unauthorized' } }
components:
  securitySchemes:
    accountContext:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Access token validated by authentication middleware; account_id comes from the trusted request context.
    deviceContext:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Short-lived device token. It is restricted to sessions explicitly created by that device and cannot authorize account APIs.
    realtimeTicket:
      type: http
      scheme: bearer
      description: HMAC-signed ticket authorizing access to one realtime Session.
  parameters:
    SessionID: { name: id, in: path, required: true, schema: { type: string } }
    MessageID: { name: message_id, in: path, required: true, schema: { type: string } }
    IdempotencyKey: { name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 1, maxLength: 200 } }
    RequestID: { name: X-Request-ID, in: header, required: false, schema: { type: string, minLength: 1, maxLength: 200 } }
  requestBodies:
    RefreshToken:
      required: true
      content: { application/json: { schema: { type: object, additionalProperties: false, required: [refresh_token], properties: { refresh_token: { type: string, minLength: 1 } } } } }
  responses:
    BadRequest: { description: Invalid request, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Unauthorized: { description: Missing or invalid authorization credentials, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Forbidden: { description: Authenticated account does not own the requested resource, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    Conflict: { description: Request conflicts with current resource state or idempotency identity, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    UnprocessableEntity: { description: Request is syntactically valid but unsupported by the service contract, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    ServiceUnavailable: { description: Required downstream service is temporarily unavailable, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    TooManyRequests: { description: Phone verification requests are temporarily rate limited, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    NotFound: { description: Resource not found, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
    RuntimeNotFound: { description: Realtime runtime not found for the requested Session, content: { application/json: { schema: { $ref: '#/components/schemas/RuntimeNotFoundError' } } } }
    NotImplemented: { description: Business implementation is intentionally pending, content: { application/json: { schema: { $ref: '#/components/schemas/Error' } } } }
  schemas:
    Device:
      type: object
      additionalProperties: false
      required: [device_id, product_id, status, bound_at, created_at]
      properties:
        device_id: { type: string, minLength: 1 }
        product_id: { type: string, minLength: 1 }
        status: { type: string, enum: [active, revoked] }
        bound_at: { type: [string, 'null'], format: date-time }
        created_at: { type: string, format: date-time }
    DevicePairingCode:
      type: object
      additionalProperties: false
      required: [pairing_code, expires_at]
      properties:
        pairing_code: { type: string, minLength: 1 }
        expires_at: { type: string, format: date-time }
    DeviceList:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/Device' } }
    PairDeviceRequest:
      type: object
      additionalProperties: false
      required: [device_id, pairing_code, signature]
      properties:
        device_id: { type: string, minLength: 1 }
        pairing_code: { type: string, minLength: 1 }
        signature: { type: string, description: Base64URL Ed25519 signature over the pairing payload. }
    DeviceAuthenticationChallengeRequest:
      type: object
      additionalProperties: false
      required: [device_id]
      properties: { device_id: { type: string, minLength: 1 } }
    DeviceAuthenticationChallenge:
      type: object
      additionalProperties: false
      required: [challenge_id, nonce, expires_at]
      properties:
        challenge_id: { type: string, minLength: 1 }
        nonce: { type: string, minLength: 1 }
        expires_at: { type: string, format: date-time }
    DeviceAuthenticationTokenRequest:
      type: object
      additionalProperties: false
      required: [device_id, challenge_id, signature]
      properties:
        device_id: { type: string, minLength: 1 }
        challenge_id: { type: string, minLength: 1 }
        signature: { type: string, description: Base64URL Ed25519 signature over the challenge payload. }
    DeviceAccessToken:
      type: object
      additionalProperties: false
      required: [access_token, expires_at, device_id]
      properties:
        access_token: { type: string, description: Short-lived device credential. Clients must not persist it beyond expires_at. }
        expires_at: { type: string, format: date-time }
        device_id: { type: string }
    RealtimeRuntimeState:
      type: string
      enum: [stopped, starting, listening, asr_processing, translating, thinking, assistant_processing, tts_processing, playing, stopping, failed]
    RealtimeRuntimeErrorCode:
      type: string
      enum: [realtime_start_failed, realtime_stop_failed, realtime_pipeline_failed, realtime_translation_rejected]
    ControlPlaneErrorCode:
      type: string
      enum: [runtime_not_found, runtime_operation_conflict, mode_not_available, mode_generation_conflict, mode_runtime_instance_mismatch, mode_operation_conflict, control_invalid_message, control_unsupported_version, control_unsupported_type, control_unauthorized_session, control_connection_closed, control_unavailable]
    RuntimeNotFoundError:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error: { $ref: '#/components/schemas/RuntimeNotFoundErrorBody' }
    RuntimeNotFoundErrorBody:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code: { type: string, enum: [runtime_not_found] }
        message: { type: string }
    RuntimeOperationConflictError:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error: { $ref: '#/components/schemas/RuntimeOperationConflictErrorBody' }
    RuntimeOperationConflictErrorBody:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code: { type: string, enum: [runtime_operation_conflict] }
        message: { type: string }
    RealtimeStartRequest:
      type: object
      additionalProperties: false
      required: [operation_id]
      properties:
        operation_id: { type: string, minLength: 1 }
        trace_id: { type: string }
        started_by: { type: string }
        initial_mode:
          $ref: '#/components/schemas/RealtimeMode'
          default: interpretation
          description: Optional for rolling compatibility. When omitted, realtime uses interpretation.
    RealtimeStopRequest:
      type: object
      additionalProperties: false
      required: [reason, ended_at]
      properties:
        trace_id: { type: string }
        reason: { type: string, enum: [user_requested, operator_cancelled, client_disconnected] }
        ended_at: { type: string, format: date-time }
    FallbackPlaybackRequest:
      type: object
      additionalProperties: false
      required: [operation_id, session_id, turn_id, target_language, translated_text, language_config_version, trace_id]
      properties:
        operation_id: { type: string, minLength: 1 }
        session_id: { type: string, minLength: 1 }
        turn_id: { type: string, minLength: 1 }
        target_language: { type: string, minLength: 1 }
        translated_text: { type: string, minLength: 1 }
        language_config_version: { type: integer, minimum: 1 }
        trace_id: { type: string, minLength: 1 }
    FallbackPlaybackReceiptStatus:
      type: string
      enum: [accepted, already_accepted]
    FallbackPlaybackReceipt:
      type: object
      additionalProperties: false
      required: [operation_id, status]
      properties:
        operation_id: { type: string, minLength: 1 }
        status: { $ref: '#/components/schemas/FallbackPlaybackReceiptStatus' }
    RealtimeRuntimeSnapshot:
      type: object
      additionalProperties: false
      required: [session_id, start_operation_id, runtime_state, current_turn_id, current_playback_id, last_error_code, updated_at]
      properties:
        session_id: { type: string }
        start_operation_id: { type: string }
        runtime_state: { $ref: '#/components/schemas/RealtimeRuntimeState' }
        current_turn_id: { type: [string, 'null'] }
        current_playback_id: { type: [string, 'null'] }
        last_error_code: { type: [string, 'null'] }
        updated_at: { type: string, format: date-time }
    RealtimeMode:
      type: string
      enum: [assistant, interpretation]
    ModePhase:
      type: string
      enum: [active, switching]
    ModeStateSnapshot:
      type: object
      additionalProperties: false
      required: [session_id, runtime_instance_id, active_mode, generation, phase, last_operation_id, updated_at]
      properties:
        session_id: { type: string, minLength: 1 }
        runtime_instance_id: { type: string, minLength: 1, maxLength: 128, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        active_mode: { $ref: '#/components/schemas/RealtimeMode' }
        generation: { type: integer, format: int64, minimum: 1 }
        phase: { $ref: '#/components/schemas/ModePhase' }
        last_operation_id: { type: [string, 'null'], maxLength: 256, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$', description: Non-null and equal to result.operation_id in a successful ControlResponse. }
        updated_at: { type: string, format: date-time }
    SwitchModeCommand:
      type: object
      additionalProperties: false
      required: [session_id, runtime_instance_id, operation_id, trace_id, expected_generation, target_mode]
      properties:
        session_id: { type: string, minLength: 1 }
        runtime_instance_id: { type: string, minLength: 1 }
        operation_id: { type: string, minLength: 1 }
        trace_id: { type: string, minLength: 1 }
        expected_generation: { type: integer, format: int64, minimum: 1 }
        target_mode: { $ref: '#/components/schemas/RealtimeMode' }
    ModeSwitchStatus:
      type: string
      enum: [applied, unchanged]
    CommandResultStatus:
      type: string
      enum: [applied, unchanged, clarification_required, unsupported, failed]
    WakeWordDetectedSignal:
      type: object
      additionalProperties: false
      description: Client-side KWS output sent over the authenticated reliable ordered DataChannel. It contains no business command or session identity.
      required: [type, event_version, signal_id, detected_at]
      properties:
        type: { type: string, const: wake_word.detected }
        event_version: { type: integer, const: 1 }
        signal_id: { type: string, minLength: 1, maxLength: 128, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        detected_at: { type: string, format: date-time }
    CommandResultEvent:
      type: object
      additionalProperties: false
      required: [type, event_version, command_id, session_id, status, message, occurred_at]
      properties:
        type: { type: string, const: command.result }
        event_version: { type: integer, const: 1 }
        command_id: { type: string, minLength: 1, maxLength: 128, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        session_id: { type: string, minLength: 1, maxLength: 128, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        runtime_instance_id: { type: string, minLength: 1 }
        generation: { type: integer, format: int64, minimum: 1 }
        status: { $ref: '#/components/schemas/CommandResultStatus' }
        action: { type: string, minLength: 1, maxLength: 64 }
        target_mode: { $ref: '#/components/schemas/RealtimeMode' }
        message: { type: string, minLength: 1, maxLength: 512 }
        occurred_at: { type: string, format: date-time }
    SwitchModeResult:
      type: object
      additionalProperties: false
      required: [operation_id, status, state]
      properties:
        operation_id: { type: string, minLength: 1, maxLength: 256, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        status: { $ref: '#/components/schemas/ModeSwitchStatus' }
        state: { $ref: '#/components/schemas/ModeStateSnapshot' }
    ControlModeSwitchCommand:
      type: object
      additionalProperties: false
      description: Session identity is bound by the authenticated PeerConnection and is never accepted from this payload.
      required: [runtime_instance_id, operation_id, expected_generation, target_mode]
      properties:
        runtime_instance_id: { type: string, minLength: 1, maxLength: 128, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        operation_id: { type: string, minLength: 1, maxLength: 256, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        expected_generation: { type: integer, format: int64, minimum: 1 }
        target_mode: { $ref: '#/components/schemas/RealtimeMode' }
    ControlModeSwitchRequest:
      type: object
      additionalProperties: false
      required: [protocol_version, type, request_id, command]
      properties:
        protocol_version: { type: integer, const: 1 }
        type: { type: string, const: mode.switch }
        request_id: { type: string, minLength: 1, maxLength: 128, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        command: { $ref: '#/components/schemas/ControlModeSwitchCommand' }
    ControlError:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code: { $ref: '#/components/schemas/ControlPlaneErrorCode' }
        message: { type: string, minLength: 1, maxLength: 512 }
    ControlResponse:
      type: object
      additionalProperties: false
      required: [protocol_version, type]
      properties:
        protocol_version: { type: integer, const: 1 }
        type: { type: string, enum: [mode.switch.result, control.error] }
        request_id: { type: string, minLength: 1, maxLength: 128, pattern: '^(?:[^\r\n\t]*\S[^\r\n\t]*)$' }
        result: { $ref: '#/components/schemas/SwitchModeResult' }
        error: { $ref: '#/components/schemas/ControlError' }
      oneOf:
        - properties: { type: { type: string, const: mode.switch.result } }
          required: [request_id, result]
          not: { required: [error] }
        - properties: { type: { type: string, const: control.error } }
          required: [error]
          not: { required: [result] }
    ModeChangedEvent:
      type: object
      additionalProperties: false
      required: [event_version, event_id, trace_id, session_id, runtime_instance_id, operation_id, from_mode, to_mode, resulting_generation, occurred_at]
      properties:
        event_version: { type: integer, const: 1 }
        event_id: { type: string, minLength: 1 }
        trace_id: { type: string, minLength: 1 }
        session_id: { type: string, minLength: 1 }
        runtime_instance_id: { type: string, minLength: 1 }
        operation_id: { type: string, minLength: 1 }
        from_mode: { $ref: '#/components/schemas/RealtimeMode' }
        to_mode: { $ref: '#/components/schemas/RealtimeMode' }
        resulting_generation: { type: integer, format: int64, minimum: 2 }
        occurred_at: { type: string, format: date-time }
    AssistantReplyEvent:
      type: object
      additionalProperties: false
      description: A finalized assistant reply. It must never be encoded as a translation FinalTurn.
      required: [event_version, event_id, trace_id, session_id, turn_id, runtime_instance_id, generation, text, language, occurred_at]
      properties:
        event_version: { type: integer, const: 1 }
        event_id: { type: string, minLength: 1 }
        trace_id: { type: string, minLength: 1 }
        session_id: { type: string, minLength: 1 }
        turn_id: { type: string, minLength: 1 }
        runtime_instance_id: { type: string, minLength: 1 }
        generation: { type: integer, format: int64, minimum: 1 }
        text: { type: string, minLength: 1 }
        language: { type: string, minLength: 1 }
        occurred_at: { type: string, format: date-time }
    VoiceSessionStatus:
      type: string
      enum: [created, active, ended, failed]
    EndVoiceSessionReason:
      type: string
      enum: [user_requested, operator_cancelled, client_disconnected]
    VoiceSessionAudioConfig:
      type: object
      additionalProperties: false
      required: [codec, sample_rate_hz, channels, echo_cancellation, noise_suppression, auto_gain_control]
      properties:
        codec: { type: string, enum: [opus] }
        sample_rate_hz: { type: integer, enum: [48000] }
        channels: { type: integer, enum: [1] }
        echo_cancellation: { type: boolean }
        noise_suppression: { type: boolean }
        auto_gain_control: { type: boolean }
    VoiceSessionCapabilities:
      type: object
      additionalProperties: false
      required: [webrtc, data_channel, microphone, speaker, speaker_diarization]
      properties:
        webrtc: { type: boolean }
        data_channel: { type: boolean }
        microphone: { type: boolean }
        speaker: { type: boolean }
        speaker_diarization: { type: boolean }
    CreateVoiceSessionRequest:
      type: object
      additionalProperties: false
      required: [capabilities]
      properties:
        audio_config: { $ref: '#/components/schemas/VoiceSessionAudioConfig' }
        capabilities: { $ref: '#/components/schemas/VoiceSessionCapabilities' }
    StartVoiceSessionRequest:
      type: object
      additionalProperties: false
      properties:
        initial_mode:
          $ref: '#/components/schemas/RealtimeMode'
          default: interpretation
          description: Optional initial runtime mode. When omitted, existing interpretation behavior is preserved.
    EndVoiceSessionRequest:
      type: object
      additionalProperties: false
      properties:
        reason: { $ref: '#/components/schemas/EndVoiceSessionReason' }
    SwitchVoiceSessionModeRequest:
      type: object
      additionalProperties: false
      required: [runtime_instance_id, expected_generation, target_mode]
      properties:
        runtime_instance_id: { type: string, minLength: 1 }
        expected_generation: { type: integer, format: int64, minimum: 1 }
        target_mode: { $ref: '#/components/schemas/RealtimeMode' }
    VoiceSession:
      type: object
      additionalProperties: false
      required: [id, account_id, status, audio_config, capabilities, started_at, ended_at, created_at]
      properties:
        id: { type: string }
        account_id: { type: string }
        status: { $ref: '#/components/schemas/VoiceSessionStatus' }
        audio_config: { $ref: '#/components/schemas/VoiceSessionAudioConfig' }
        capabilities: { $ref: '#/components/schemas/VoiceSessionCapabilities' }
        started_at: { type: [string, 'null'], format: date-time }
        ended_at: { type: [string, 'null'], format: date-time }
        created_at: { type: string, format: date-time }
    VoiceSessionDetail:
      type: object
      additionalProperties: false
      required: [id, account_id, status, runtime_state, audio_config, capabilities, current_turn_id, current_playback_id, last_error_code, retryable, runtime_updated_at, started_at, ended_at, created_at]
      properties:
        id: { type: string }
        account_id: { type: string }
        status: { $ref: '#/components/schemas/VoiceSessionStatus' }
        runtime_state: { $ref: '#/components/schemas/RealtimeRuntimeState' }
        audio_config: { $ref: '#/components/schemas/VoiceSessionAudioConfig' }
        capabilities: { $ref: '#/components/schemas/VoiceSessionCapabilities' }
        current_turn_id: { type: [string, 'null'] }
        current_playback_id: { type: [string, 'null'] }
        last_error_code: { type: [string, 'null'] }
        retryable: { type: boolean }
        runtime_updated_at: { type: string, format: date-time }
        started_at: { type: [string, 'null'], format: date-time }
        ended_at: { type: [string, 'null'], format: date-time }
        created_at: { type: string, format: date-time }
    VoiceSessionListItem:
      type: object
      additionalProperties: false
      required: [id, account_id, status, started_at, ended_at, created_at]
      properties:
        id: { type: string }
        account_id: { type: string }
        status: { $ref: '#/components/schemas/VoiceSessionStatus' }
        started_at: { type: [string, 'null'], format: date-time }
        ended_at: { type: [string, 'null'], format: date-time }
        created_at: { type: string, format: date-time }
    VoiceSessionListPage:
      type: object
      additionalProperties: false
      required: [sessions, next_cursor]
      properties:
        sessions: { type: array, items: { $ref: '#/components/schemas/VoiceSessionListItem' } }
        next_cursor: { type: [string, 'null'] }
    VoiceSessionStateSnapshot:
      type: object
      additionalProperties: false
      required: [session_id, status, runtime_state, current_turn_id, current_playback_id, last_error_code, retryable, runtime_updated_at]
      properties:
        session_id: { type: string }
        status: { $ref: '#/components/schemas/VoiceSessionStatus' }
        runtime_state: { $ref: '#/components/schemas/RealtimeRuntimeState' }
        current_turn_id: { type: [string, 'null'] }
        current_playback_id: { type: [string, 'null'] }
        last_error_code: { type: [string, 'null'] }
        retryable: { type: boolean }
        runtime_updated_at: { type: string, format: date-time }
    InterpretationOutputMode:
      type: string
      enum: [bidirectional, single]
    LanguagePair:
      type: object
      additionalProperties: false
      required: [source, target]
      properties:
        source: { type: string }
        target: { type: string }
    OutputRoute:
      type: object
      additionalProperties: false
      required: [target_language, tts_enabled, delivery_enabled]
      properties:
        target_language: { type: string }
        tts_enabled: { type: boolean }
        delivery_enabled: { type: boolean }
    SupportedLanguage:
      type: object
      additionalProperties: false
      required: [language_code, display_name, display_name_en, supports_as_source, supports_as_target]
      properties:
        language_code: { type: string }
        display_name: { type: string }
        display_name_en: { type: string }
        supports_as_source: { type: boolean }
        supports_as_target: { type: boolean }
    ListLanguagesResponse:
      type: object
      additionalProperties: false
      required: [languages]
      properties:
        languages: { type: array, items: { $ref: '#/components/schemas/SupportedLanguage' } }
    CreateLanguageConfigRequest:
      type: object
      additionalProperties: false
      required: [languages]
      properties:
        languages: { type: array, minItems: 1, items: { $ref: '#/components/schemas/LanguagePair' } }
        output_routes: { type: array, minItems: 1, maxItems: 2, items: { $ref: '#/components/schemas/OutputRoute' } }
        expected_version: { type: integer, minimum: 1 }
    LanguageConfig:
      type: object
      additionalProperties: false
      required: [id, session_id, version, language_pairs, output_routes, output_mode, status, effective_from, effective_until, created_by, created_at]
      properties:
        id: { type: string }
        session_id: { type: string }
        version: { type: integer, minimum: 1 }
        language_pairs: { type: array, items: { $ref: '#/components/schemas/LanguagePair' } }
        output_routes: { type: array, items: { $ref: '#/components/schemas/OutputRoute' } }
        output_mode: { $ref: '#/components/schemas/InterpretationOutputMode' }
        status: { type: string, enum: [active, superseded, expired] }
        effective_from: { type: string, format: date-time }
        effective_until: { type: [string, 'null'], format: date-time }
        created_by: { type: string }
        created_at: { type: string, format: date-time }
    ListLanguageConfigsResponse:
      type: object
      additionalProperties: false
      required: [items, next_cursor]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/LanguageConfig' } }
        next_cursor: { type: [string, 'null'] }
    RealtimeTicket:
      type: object
      additionalProperties: false
      required: [ticket, session_id, expires_at]
      properties:
        ticket: { type: string, description: HMAC-signed realtime bearer ticket }
        session_id: { type: string }
        expires_at: { type: string, format: date-time }
    WebRTCConnectionState:
      type: string
      enum: [new, connecting, connected, disconnected, failed, closed]
    WebRTCErrorCode:
      type: string
      enum: [webrtc_connection_not_found, webrtc_connection_not_ready, webrtc_connection_failed]
    WebRTCConnectionError:
      type: object
      additionalProperties: false
      required: [error]
      properties:
        error: { $ref: '#/components/schemas/WebRTCConnectionErrorBody' }
    WebRTCConnectionErrorBody:
      type: object
      additionalProperties: false
      required: [code, message]
      properties:
        code: { $ref: '#/components/schemas/WebRTCErrorCode' }
        message: { type: string }
    WebRTCConnectionSnapshot:
      type: object
      additionalProperties: false
      required: [session_id, connection_id, state, version, updated_at]
      properties:
        session_id: { type: string }
        connection_id: { type: string }
        state: { $ref: '#/components/schemas/WebRTCConnectionState' }
        version: { type: integer, format: int64, minimum: 1 }
        updated_at: { type: string, format: date-time }
    WebRTCConfig: { type: object, additionalProperties: false, required: [session_id, expires_at, ice_servers, ice_transport_policy, data_channel, control_data_channel, audio], properties: { session_id: { type: string }, expires_at: { type: string, format: date-time }, ice_servers: { type: array, items: { $ref: '#/components/schemas/WebRTCICEServer' } }, ice_transport_policy: { type: string, enum: [all] }, data_channel: { $ref: '#/components/schemas/WebRTCDataChannelConfig' }, control_data_channel: { $ref: '#/components/schemas/WebRTCControlDataChannelConfig' }, audio: { $ref: '#/components/schemas/WebRTCAudioConfig' } } }
    WebRTCICEServer: { type: object, additionalProperties: false, required: [urls], properties: { urls: { type: array, items: { type: string } }, username: { type: string }, credential: { type: string } } }
    WebRTCDataChannelConfig: { type: object, additionalProperties: false, required: [label, ordered], properties: { label: { type: string }, ordered: { type: boolean } } }
    WebRTCControlDataChannelConfig: { type: object, additionalProperties: false, required: [label, ordered, protocol_version], properties: { label: { type: string, const: lingow-control-v1 }, ordered: { type: boolean, const: true }, protocol_version: { type: integer, const: 1 } } }
    WebRTCAudioConfig: { type: object, additionalProperties: false, required: [uplink_codec, downlink_codec, sample_rate_hz, channels], properties: { uplink_codec: { type: string }, downlink_codec: { type: string }, sample_rate_hz: { type: integer, minimum: 1 }, channels: { type: integer, minimum: 1 } } }
    Account:
      type: object
      required: [id, kind, created_at]
      properties: { id: { type: string }, kind: { type: string, enum: [anonymous, registered] }, created_at: { type: string, format: date-time } }
    Tokens:
      type: object
      required: [access_token, refresh_token, expires_at]
      properties: { access_token: { type: string }, refresh_token: { type: string }, expires_at: { type: string, format: date-time } }
    AuthResult:
      type: object
      required: [account, tokens]
      properties: { account: { $ref: '#/components/schemas/Account' }, tokens: { $ref: '#/components/schemas/Tokens' } }
    PhoneChallengeRequest:
      type: object
      additionalProperties: false
      required: [phone]
      properties: { phone: { type: string, minLength: 1 } }
    PhoneVerifyRequest:
      type: object
      additionalProperties: false
      required: [challenge_id, code]
      properties:
        challenge_id: { type: string }
        code: { type: string }
        anonymous_account_id:
          type: string
          description: When supplied, the request must include a Bearer access token for this same anonymous account.
    Stage: { type: string, enum: [asr, translation, assistant_llm, tts, diarization] }
    UsageSummary:
      type: object
      required: [account_id, period_start, period_end, totals]
      properties:
        account_id: { type: string }
        session_id: { type: string }
        period_start: { type: string, format: date-time }
        period_end: { type: string, format: date-time }
        totals:
          type: array
          items:
            type: object
            required: [service_type, input_tokens, output_tokens, audio_duration_ms, cost_amount, currency]
            properties:
              service_type: { $ref: '#/components/schemas/Stage' }
              input_tokens: { type: integer, format: int64, minimum: 0 }
              output_tokens: { type: integer, format: int64, minimum: 0 }
              audio_duration_ms: { type: integer, format: int64, minimum: 0 }
              cost_amount: { type: string }
              currency: { type: string }
    Channel: { type: string, enum: [email, wechat] }
    CreateMessageRequest:
      type: object
      additionalProperties: false
      required: [channel, destination_ref, turn_ids]
      properties: { channel: { $ref: '#/components/schemas/Channel' }, destination_ref: { type: string }, turn_ids: { type: array, minItems: 1, maxItems: 100, uniqueItems: true, items: { type: string } } }
    FinalTurnSnapshot:
      type: object
      required: [turn_id, session_id, participant_id, speaker_label_snapshot, source_language, target_language, language_config_version, source_text, translated_text, created_at]
      properties:
        turn_id: { type: string }
        session_id: { type: string }
        participant_id: { type: [string, 'null'] }
        speaker_label_snapshot: { type: [string, 'null'] }
        source_language: { type: string }
        target_language: { type: string }
        language_config_version: { type: integer, format: int64, minimum: 1 }
        source_text: { type: string }
        translated_text: { type: string }
        created_at: { type: string, format: date-time }
    Message:
      type: object
      required: [id, account_id, channel, destination_ref, snapshot_version, turns, status, attempts, last_error_code, created_at, updated_at]
      properties:
        id: { type: string }
        account_id: { type: string }
        channel: { $ref: '#/components/schemas/Channel' }
        destination_ref: { type: string }
        snapshot_version: { type: integer, minimum: 1 }
        turns: { type: array, items: { $ref: '#/components/schemas/FinalTurnSnapshot' } }
        status: { type: string, enum: [queued, sending, sent, failed, retrying, cancelled] }
        attempts: { type: integer, minimum: 0 }
        last_error_code: { type: [string, 'null'] }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }
    OutboundMessageListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/Message' } }
    Preference:
      type: object
      required: [account_id, channel, destination_ref, enabled, verified, updated_at]
      properties: { account_id: { type: string }, channel: { $ref: '#/components/schemas/Channel' }, destination_ref: { type: string }, enabled: { type: boolean }, verified: { type: boolean }, updated_at: { type: string, format: date-time } }
    MessagePreferenceListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/Preference' } }
    UpdateMessagePreferenceRequest:
      type: object
      additionalProperties: false
      required: [enabled]
      properties:
        enabled: { type: boolean }
    AutomaticOutputStatus:
      type: object
      additionalProperties: false
      required: [turn_id, status, updated_at]
      properties:
        turn_id: { type: string }
        status:
          type: string
          enum: [pending, succeeded, partially_succeeded, failed, fallback_pending, fallback_played, restored]
        updated_at: { type: string, format: date-time }
    AutomaticOutputStatusListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/AutomaticOutputStatus' } }
    AutomaticDeliveryReadiness:
      type: object
      additionalProperties: false
      required: [ready]
      properties:
        ready: { type: boolean }
    MessageTarget:
      type: object
      required: [destination_ref, channel, verified, updated_at]
      properties:
        destination_ref: { type: string }
        channel: { $ref: '#/components/schemas/Channel' }
        verified: { type: boolean }
        revoked_at: { type: [string, 'null'], format: date-time }
        updated_at: { type: string, format: date-time }
    MessageTargetListResponse:
      type: object
      additionalProperties: false
      required: [items]
      properties:
        items: { type: array, items: { $ref: '#/components/schemas/MessageTarget' } }
    BindWeChatRequest:
      type: object
      additionalProperties: false
      required: [code]
      properties:
        code: { type: string, minLength: 1, description: 'WeChat OAuth login code exchanged server-side for OpenID' }
    BindEmailRequest:
      type: object
      additionalProperties: false
      required: [token]
      properties:
        token: { type: string, minLength: 1, description: 'One-time email verification token; dev mode accepts dev: prefixed addresses' }
    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message, request_id, retryable, details]
          properties:
            code: { type: string }
            message: { type: string }
            request_id: { type: string }
            retryable: { type: boolean }
            details: { type: object, additionalProperties: true }
