# 语音播放状态与模式切换问题

## 问题一：助手回复成功但提示失败

现象：唤醒词触发助手后，日志已出现 `stage=assistant_reply_done`，但随后因 TTS 播放被新的输出 `superseded`，前端显示“助手暂时无法回答，请重试”。

原因：模型生成成功，播放阶段被取消，却被统一归类为执行失败；`assistant_query` 对所有执行错误使用同一条失败提示。

方案：

- 将 `superseded`、用户主动取消等可预期中断与模型/TTS 实际失败分开分类。
- 可预期中断不显示“助手无法回答”，仅结束当前播放状态或提示“回答已中断”。
- 只有模型生成、TTS 生成等真实失败才返回重试提示。

## 问题二：切换同声传译后前几句 TTS 不播放

现象：模式切换后翻译和 TTS 已产出音频，但首批音频提交时出现 `playback is not active`；等待一段时间后，后续 TTS 恢复正常。

原因：模式切换立即中断旧播放。旧播放清理、运行时 generation 更新与新模式首批 TTS 同时发生，导致新音频使用了已失效或不再 active 的 `playback_id`。

方案：

- 为播放状态绑定 session/generation，模式切换时先取消并完成旧播放清理。
- 新模式确认成为 active 后，再创建新的 `playback_id` 并允许提交 TTS 音频。
- 对旧 generation 的延迟音频块静默丢弃，避免污染新播放状态。
- `playback is not active` 对已取消的旧播放按预期竞态处理，不向用户显示为翻译失败。

验收：模式切换后新模式首个 TTS 可播放；旧播放的延迟事件不会影响新模式；真实模型或 TTS 失败仍显示明确的重试提示。
